# Capy Notes · 实施路线图

> 分阶段计划。P2 结束即是一个可日常使用的产品；P3 为增强项，按需取舍。
> 每个阶段结束都有明确验收标准（DoD）。

## P0 · 笔记基础（1~2 周）

**目标：能记笔记、能搜笔记。**

| 任务 | 产出 | 验收 |
|---|---|---|
| Flutter 项目脚手架（client/） | `flutter create` + Riverpod + go_router + drift 接入 | `flutter run` 正常启动 |
| 数据层 | notes 表 + FTS5 + DAO + 迁移 | 单测覆盖 CRUD |
| 笔记列表页 | 文件夹/标签分组、搜索框 | 列表可浏览、可按关键字过滤 |
| 编辑器 | 源码编辑 + 预览双模式、自动保存 | 编辑→保存→重启不丢数据 |
| 设置页 | 基本设置骨架 | 可进入 |

**DoD**：可以创建/编辑/搜索/删除笔记，重启后数据完整。

## P1 · AI 接入（1 周）

**目标：打通云端 AI，单笔记对话。**

| 任务 | 产出 | 验收 |
|---|---|---|
| `AiClient` 统一封装 | OpenAI 兼容客户端（chat + embedding） | 设置页填 Key 后能完成一次对话 |
| `ChatService` | SSE 流式输出 | 打字机效果，可中断 |
| `ChunkingService` | 按标题/段落切块（500~800 token + 重叠） | 单元测试覆盖切块边界 |
| `EmbeddingService` | 增量嵌入 + chunks 表写入 | 编辑笔记后自动同步变更块 |
| 单笔记对话页 | 针对单篇笔记提问 | 回答引用该笔记内容，可跳转 |

**DoD**：对单篇笔记提问，AI 基于该笔记内容回答并给出引用。

## P2 · 全库 RAG（1~2 周）

**目标：全库知识库问答。**

| 任务 | 产出 | 验收 |
|---|---|---|
| sqlite-vec 接入 | 向量列 + 相似度检索 | 移动端可运行 |
| 混合检索 | FTS5 + 向量 + RRF 融合 | 对比纯向量，专有名词召回率提升 |
| `RAGService` | 检索→组装 Prompt→流式生成 | 回答带引用 [笔记标题]，点击跳转原文高亮 |
| `in_kb` 管理 | 设置页控制知识库范围 | 关闭后笔记不参与检索 |
| 知识库对话页 | 全库问答入口 | 端到端可用 |

**DoD**：对全库提问，回答准确引用相关笔记，引用可跳转，成本可控（增量嵌入生效）。

## P3 · 增强（持续迭代，按需取舍）

| 方向 | 内容 | 备注 |
|---|---|---|
| 编辑器演进 | appflowy_editor 块级 WYSIWYG | 用户有强需求再上 |
| 同步 | WebDAV / 坚果云多端同步 | 冲突策略：LWW |
| 双链/图谱 | `[[链接]]` 双链 + 反向链接 | 对齐 Obsidian 心智 |
| 模板 | 日记/会议/读书笔记模板 | 复用 chunking 基础设施 |
| 导出 | Markdown / PDF 导出 | 见己项目已有类似实现可参考 |
| 订阅/会员 | 可选后端（见己项目复用 license 体系） | 若商业化 |

## 里程碑

```
P0 ──→ P1 ──→ P2 ──→ 可日常使用
              │
              └──→ P3 增强项（按需）
```
